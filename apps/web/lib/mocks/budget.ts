import { expenseBudgets } from './expense'
import { lifeGoals } from './dashboard'

export const budgetPageMock = {
  monthlyIncome: 280000,
  savingTarget: 60000,
  budgets: expenseBudgets,
  goals: lifeGoals,
}
